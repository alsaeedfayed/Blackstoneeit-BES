import { MembersCountPipe } from './members-count.pipe';

describe('MembersCountPipe', () => {
  it('create an instance', () => {
    const pipe = new MembersCountPipe();
    expect(pipe).toBeTruthy();
  });
});
