import { MeetingTimeLeftPipe } from './meeting-time-left.pipe';

describe('MeetingTimeLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingTimeLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
