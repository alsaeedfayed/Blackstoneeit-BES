import { HoursLeftPipe } from './hours-left.pipe';

describe('HoursLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
