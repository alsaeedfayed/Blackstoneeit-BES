import { RichTextToTextPipe } from './rich-text-to-text.pipe';

describe('RichTextToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new RichTextToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
