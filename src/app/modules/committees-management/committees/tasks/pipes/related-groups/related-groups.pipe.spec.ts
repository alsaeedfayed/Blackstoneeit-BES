import { RelatedGroupsPipe } from './related-groups.pipe';

describe('RelatedGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new RelatedGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
