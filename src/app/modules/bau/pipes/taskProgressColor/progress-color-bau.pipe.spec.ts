import { ProgressColorBauPipe } from './progress-color-bau.pipe';

describe('ProgressColorBauPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressColorBauPipe();
    expect(pipe).toBeTruthy();
  });
});
