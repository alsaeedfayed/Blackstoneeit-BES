import { TableItemIndexPipe } from './table-item-index.pipe';

describe('TableItemIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new TableItemIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
