import { EnumKeyByValuePipe } from './enum-key-by-value.pipe';

describe('EnumKeyByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeyByValuePipe();
    expect(pipe).toBeTruthy();
  });
});
