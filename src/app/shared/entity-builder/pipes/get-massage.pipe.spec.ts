import { GetMassagePipe } from './get-massage.pipe';

describe('GetMassagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMassagePipe();
    expect(pipe).toBeTruthy();
  });
});
