import { IsSupportItemsPipe } from './is-support-items.pipe';

describe('IsSupportItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSupportItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
