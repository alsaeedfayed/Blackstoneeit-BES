export interface IButtonSelect {
    text: { en: string, ar: string };
    className: string;
    clicked?: boolean;
    // background?: string;
    // color?: string;
    // onClick: (params?: any) => void;
}